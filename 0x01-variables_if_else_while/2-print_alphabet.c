#include <stdio.h>
/**
 * Main - entry point
 *
 * Description: Pint the lowercase alphabet
 * Return: 0
 */
int main(void)
{
int c;

for (c = 0; c < 26; ++c)
putchar('n' + c);

putchar('\n');

return (0);
}
